import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export default function AccountScreen() {
  return (
    <PlaceholderScreen
      title="Account"
      description="Profile, friends, groups, theme, and visibility defaults."
      landsIn="Auth lands in A1.3 · friends and groups in T4"
    />
  );
}
